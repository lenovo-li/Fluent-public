# 贡献指南

先说清项目现状：这是一个单人从零手写的框架，没有 ABI 稳定性承诺，
API 仍可能变。欢迎 issue 和 PR，但请先读完本文，尤其是「新控件的准入标准」
和「不可妥协的约束」两节——不满足这两条的 PR 无法合并，哪怕功能是对的。

## 环境

- Visual Studio 2022+、Windows 10 1803+ SDK
- 仅 x64（Win32/x86、ARM64 未构建测试）
- 无第三方依赖：不要引入 gtest / boost / fmt 之类的库，测试框架是自研的
  （`tests/framework/Test.h`，只有 `EXPECT_TRUE` / `EXPECT_FALSE` /
  `EXPECT_EQ` / `EXPECT_NE` / `EXPECT_NEAR` 五个宏）

构建与测试命令见 `README.md`。

## 不可妥协的约束

这两条违反了就是 bug，不接受「先合并再优化」：

1. **不允许在 UI 线程做耗时操作。** 任何会卡住 UI 线程的调用（同步 IO、
   网络、大循环）都不行。用户体验对卡顿极其敏感，而卡顿一旦进主干就很难
   定位回哪次改动。
2. **控件不许硬编码颜色、圆角、字号、间距。** 一律走 `Theme()` 读 token，
   否则暗色主题下必然出错，而这种错在亮色主题下测不出来。

## 新控件的准入标准

新控件需要满足：

- **布局正确**：Measure/Arrange 两趟正确实现，返回值反映实际尺寸
- **DirtyFlags 标注正确**：Measure 级脏标记（影响布局）和 Render 级脏标记（仅重绘）不能混
- **可访问性**：控件可以通过键盘聚焦和操作
- **状态完整**：禁用态（IsEnabled=false）有正确的视觉表现
- **主题 token 化**：所有颜色、字号、圆角、间距从 `Theme()` 读取
- **有测试**：至少覆盖基本交互和边界情况（空输入、禁用态、主题切换）
- **注册到两个构建系统**：`.vcxproj` **和** `CMakeLists.txt` 都要加

最后一条特别容易漏：两个构建系统都没有用 glob，新增 `.cpp` 必须手工加到
两处。只加 vcxproj 的话你本地一切正常，而只用 CMake 的人会拿到一串
`LNK2019` 未解析外部符号。

运行 `python scripts/check_all.py` 可以检查源文件列表是否同步。

## 测试要求

新功能和 bug 修复都要有测试。两条经验：

**断言差异，不要断言绝对值。** 比如验证「空输入框有光标」，
断言「聚焦时的像素比未聚焦时多」，而不是「某个坐标有 N 个深色像素」——
后者换个字号就假失败，而且可能根本没测到你以为的东西。

**做变异测试。** 写完测试后，故意把修复改回坏的样子，确认测试**真的变红**。
一个在坏代码上也通过的测试等于没有。

视觉正确性（闪烁、重影、平滑度）无法用 headless 测试验证。涉及滚动、动画、
渲染的改动，请在 PR 里说明你在真机上看了哪个界面、观察到什么。

## PR 流程

1. 从 `master` 开分支，分支名用 `feat/`、`fix/`、`docs/`、`chore/` 前缀
2. Debug + Release 双配置干净编译，`x64/Debug/FluentUITests.exe` 全绿
3. 性能相关改动跑 `x64/Release/FluentUIBench.exe --gate`，确认 exit 0
4. 提交信息说明**为什么**这么改，不要只说改了什么——半年后回头看，
   「为什么」是唯一救得了你的东西

## 报 issue

请附：Windows 版本、VS 版本、显示器刷新率与 DPI 缩放（渲染与卡顿问题几乎
都和这三个有关）、复现步骤。视觉问题请附截图。

## 代码风格

- 遵循 `.editorconfig` 的配置（缩进、换行、编码）
- C++20 特性随意用（ranges / concepts / coroutines 都可以）
- 优先用标准库（`std::span` / `std::optional`），不要重复造轮子
- 注释写**为什么**，不要重复代码做了什么

## 性能约束

本框架对性能有明确标准：

- **Resize 流畅性**：窗口拖动 resize 全程保持 60 FPS（`x64/Release/FluentUIBench.exe --gate` 有 renderMs 上限检查）
- **滚动平滑度**：ListBox / TreeView / TextArea 的合成器滚动在 UI 线程繁忙时也不掉帧
- **idle 零 CPU**：无轮询，消息循环空闲时阻塞等待
- **启动速度**：TextArea 打开 28 MB / 20 万行文本秒开（NoWrap 模式精确虚拟化）

性能相关的改动（布局算法、渲染路径、虚拟化逻辑）请在 PR 里说明你测了什么场景、
帧时间和 CPU 占用的变化。

## 联系

- Issue：功能建议、bug 报告、架构讨论
- PR：直接提交代码，大改动建议先开 issue 讨论
